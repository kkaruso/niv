test:pylint:
  image: python:3.9
  before_script:
    - python -V # Print out python version for debugging
  script:
    - pip install pylint --quiet
    - pip install pyyaml
    - pylint --ignored-classes=_socketobject --max-line-length=150 *.py
    - pylint --max-statements=100 *.py

test:pytest:
  image: python:3.9
  before_script:
    - python -V # Print out python version for debugging
  script:
    - pip install pytest --quiet
    - pip install pyyaml
    - pip install graphviz
    - apt-get update && apt-get upgrade -y graphviz
    - python -m pytest
    

